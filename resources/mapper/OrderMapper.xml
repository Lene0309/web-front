<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nikki.finalproject.mapper.OrderMapper">

    <!-- 结果映射 -->
    <resultMap id="OrderResultMap" type="com.nikki.finalproject.entity.Order">
        <id column="order_id" property="orderId" jdbcType="VARCHAR"/>
        <result column="customer_id" property="customerId" jdbcType="VARCHAR"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="total_amount" property="totalAmount" jdbcType="DECIMAL"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="receiver_name" property="receiverName" jdbcType="VARCHAR"/>
        <result column="receiver_phone" property="receiverPhone" jdbcType="VARCHAR"/>
        <result column="shipping_address" property="shippingAddress" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 查询订单列表 -->
    <select id="selectOrderList" resultMap="OrderResultMap">
        SELECT 
            o.order_id,
            o.customer_id,
            o.username,
            o.total_amount,
            o.status,
            o.receiver_name,
            o.receiver_phone,
            o.shipping_address,
            o.create_time,
            o.update_time
        FROM orders o
        <where>
            <if test="orderNo != null and orderNo != ''">
                AND o.order_id LIKE CONCAT('%', #{orderNo}, '%')
            </if>
            <if test="username != null and username != ''">
                AND o.username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="status != null">
                AND o.status = #{status}
            </if>
        </where>
        ORDER BY o.create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 统计订单总数 -->
    <select id="countOrderList" resultType="int">
        SELECT COUNT(*)
        FROM orders o
        <where>
            <if test="orderNo != null and orderNo != ''">
                AND o.order_id LIKE CONCAT('%', #{orderNo}, '%')
            </if>
            <if test="username != null and username != ''">
                AND o.username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="status != null">
                AND o.status = #{status}
            </if>
        </where>
    </select>

    <!-- 根据订单ID查询订单 -->
    <select id="selectOrderById" resultMap="OrderResultMap">
        SELECT 
            o.order_id,
            o.customer_id,
            o.username,
            o.total_amount,
            o.status,
            o.receiver_name,
            o.receiver_phone,
            o.shipping_address,
            o.create_time,
            o.update_time
        FROM orders o
        WHERE o.order_id = #{orderId}
    </select>

    <!-- 插入订单 -->
    <insert id="insertOrder" parameterType="com.nikki.finalproject.entity.Order">
        INSERT INTO orders (
            order_id,
            customer_id,
            username,
            total_amount,
            status,
            receiver_name,
            receiver_phone,
            shipping_address,
            create_time,
            update_time
        ) VALUES (
            #{orderId},
            #{customerId},
            #{username},
            #{totalAmount},
            #{status},
            #{receiverName},
            #{receiverPhone},
            #{shippingAddress},
            NOW(),
            NOW()
        )
    </insert>

    <!-- 更新订单 -->
    <update id="updateOrder" parameterType="com.nikki.finalproject.entity.Order">
        UPDATE orders
        SET 
            customer_id = #{customerId},
            username = #{username},
            total_amount = #{totalAmount},
            status = #{status},
            receiver_name = #{receiverName},
            receiver_phone = #{receiverPhone},
            shipping_address = #{shippingAddress},
            update_time = NOW()
        WHERE order_id = #{orderId}
    </update>

    <!-- 更新订单状态 -->
    <update id="updateOrderStatus">
        UPDATE orders
        SET 
            status = #{status},
            update_time = NOW()
        WHERE order_id = #{orderId}
    </update>

    <!-- 删除订单 -->
    <delete id="deleteOrder">
        DELETE FROM orders WHERE order_id = #{orderId}
    </delete>

</mapper> 